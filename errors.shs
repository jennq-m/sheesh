toptier() {
    if (test) {
        test = test;
    } ex (test) {
        test = test;
    } ex (test) {
        test = test;
    } other {
        if (test) {
            hello;
        } other {
            hello;
        }
    }
}